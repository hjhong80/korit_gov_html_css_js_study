// block현상을 해결하기 위해 비동기라는 방식 탄생

setTimeout(() => {
    console.log("작업 2");
}, 3000);                       // 3000밀리초 -> 3초

console.log("작업 1");

// 작업 진행 순서 : 3초간 타임아웃 실행 -> 작업 1 출력 실행 -> 3초후 작업 2 출력 실행
// 작업 1 실행 전에 3초 카운트가 이미 시작되었음에 주의!!

// single thread 방식인 javascript에서 비동기 실행이 가능한 이유?
// web api라는 엔진이 별도로 존재하여 비동기 실행을 도와준다.